import { ActionType } from '../action-types';
import { FormProps } from '../../components/auth/signup';
import { Dispatch } from 'redux';


export const signup = ({ email, password }: FormProps) => {
  return async (dispatch: Dispatch) => {
    
  }
};